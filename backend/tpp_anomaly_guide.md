# Tulip 2.0 Anomaly Feature Guide

This guide is generated by the configured local LLM from `tulip2.0_schema.md`.
Python validates that every selected table, column, and feature operation exists before writing runtime config.

## Validation Context Files

- `/home/ruthikreddy/Documents/validation_files/THIRD_PARTY_PAYMENTS_VALIDATION.md`

## LLM Architecture Notes

### dak

This source plan is built for the `dak` table as the root of THIRD_PARTY_PAYMENTS (fk_section IN (142, 228, 265, 383)). It leverages all available columns, especially runtime workflow features, to detect anomalies in flow, status, amount, date, and identity. The plan prioritizes deterministic, audit-explainable features that expose missing evidence, broken flow, and contradictory states. All features are derived from visible columns and strictly follow the feature_contract. This plan is designed to feed Isolation Forest with high-signal forensic features.

### bill

This feature plan is designed for the `bill` table as the primary source for detecting anomalies in third-party payments. It leverages validation rules, workflow logic, and forensic patterns to expose hidden risks such as broken flow, approval contradictions, amount inconsistencies, duplicate invoice behavior, and payment routing issues. All features are deterministic, numeric, and explainable to auditors. The plan prioritizes flow integrity, amount validation, and identity consistency across related records.

### cheque_slip

This source plan focuses on the final payment stage (cheque_slip) in the third-party payment flow. It detects anomalies in payment execution, including missing identities, invalid dates, duplicate or repeated patterns, amount inconsistencies, and suspicious payment details. All features are deterministic, numeric, and explainable to an auditor. The plan leverages upstream DAK context via FK relationships to enforce flow integrity and detect broken or skipped stages.

### punching_medium

This source plan focuses on punching_medium as the final payment flow table in the third-party payment chain. It detects anomalies in payment timing, identity reuse, duplicate entries, amount outliers, and flow breaks. All features are deterministic, numeric, and explainable to auditors. The plan prioritizes broken flow, skipped stages, downstream without upstream, and weak final payment trails. It leverages FK relationships to dak for context enrichment and uses normalized keys to detect subtle identity mismatches.

### schedule3

schedule3 is the final stage in the third-party payment flow. It must be linked to a valid DAK and cheque slip. All features focus on flow integrity, amount consistency, date chronology, and duplicate or abnormal payment patterns. Use this source to detect broken trails, overpayments, and duplicate finalizations.

### ecs

ECS is the final stage of the third-party payment flow. This source plan focuses on detecting broken flow, skipped stages, approval contradictions, payment amount inconsistencies, duplicate or split payment patterns, and abnormal timing. All features are deterministic, numeric, and explainable to an auditor. The plan leverages FK relationships to DAK, schedule3, and cheque_slip for forensic context. Use only visible columns from full_usable_columns. No LLM reasoning is required at scan time.

## Runtime Sources And Feature Plans

### dak

- ID column: `id`
- Amount column: `amount`
- Context columns: `dakid_no, list_no, reference_no, bill_no, fk_section, fk_central_unit, month_year, subject, record_status, created_at, reference_date, list_date, bill_date`
- Date column: `created_at`
- Why this source: dak is the root table for THIRD_PARTY_PAYMENTS (fk_section IN (142, 228, 265, 383)) and contains primary identity, amount, status, and chronology. It drives downstream flow and is enriched with runtime workflow features for flow integrity checks.

Feature plan:
- `is_missing_dakid_no` (`is_missing`): DAK identifier is required for traceability; missing DAKID indicates invalid or unregistered payment entry.
- `is_missing_list_no` (`is_missing`): List number is required for DAK traceability; its absence may indicate incomplete or forged entry.
- `is_missing_reference_no` (`is_missing`): Reference number from vendor/authority is a key alternate identifier; missing value may indicate fake or unverified source.
- `is_missing_bill_no` (`is_missing`): Bill number is required for third-party payments; missing value suggests unverified or fabricated bill.
- `is_missing_bill_date` (`is_missing`): Bill date is required; missing date breaks chronology and may indicate manipulation.
- `is_missing_list_date` (`is_missing`): List date is required for DAK chronology; missing value undermines audit trail.
- `is_missing_reference_date` (`is_missing`): Reference date is required for document evidence; missing value weakens authenticity.
- `is_missing_amount` (`is_missing`): Amount is required for payment validation; missing value indicates incomplete or invalid entry.
- `is_missing_month_year` (`is_missing`): Month/year context is required for accounting and approval power slab; missing value breaks fiscal traceability.
- `is_missing_record_status` (`is_missing`): Record status is required for validity; missing status prevents flow and approval tracking.
- `is_missing_created_at` (`is_missing`): Creation timestamp is required for workflow chronology; missing value breaks recency and audit trail.
- `is_missing_fk_section` (`is_missing`): Section ID is required to filter only THIRD_PARTY_PAYMENTS; missing value indicates invalid scope.
- `is_missing_fk_central_unit` (`is_missing`): Central unit reference is required for unit-level accountability; missing value breaks traceability.
- `is_missing_subject` (`is_missing`): Subject is required for payment purpose context; missing value weakens narrative traceability.
- `is_missing_bill_date_when_bill_no` (`missing_when_present`): When bill number exists, bill date must be present; missing date breaks chronology.
- `is_missing_reference_date_when_reference_no` (`missing_when_present`): When reference number exists, reference date must be present; missing date breaks evidence chain.
- `is_missing_list_date_when_list_no` (`missing_when_present`): When list number exists, list date must be present; missing date breaks DAK chronology.
- `is_missing_dakid_no_when_record_status_valid` (`missing_when_equals`): Valid DAK records (record_status='V') must have a DAKID; missing DAKID indicates invalid entry.
- `is_missing_bill_no_when_record_status_valid` (`missing_when_equals`): Valid DAK records must have a bill number; missing bill number indicates invalid entry.
- `is_missing_amount_when_record_status_valid` (`missing_when_equals`): Valid DAK records must have an amount; missing amount indicates invalid entry.
- `is_missing_month_year_when_record_status_valid` (`missing_when_equals`): Valid DAK records must have month/year context; missing value breaks fiscal traceability.
- `is_missing_fk_central_unit_when_record_status_valid` (`missing_when_equals`): Valid DAK records must have a central unit; missing unit breaks accountability.
- `is_missing_subject_when_record_status_valid` (`missing_when_equals`): Valid DAK records must have a subject; missing subject breaks purpose context.

### bill

- ID column: `id`
- Amount column: `amount_passed`
- Context columns: `fk_dak, fk_central_vendor, fk_central_unit, invoice_number, invoice_date, payment_mode, payment_record_type, gst_applicable, fk_bill_type, fk_section, created_at, approved, passed, record_status, ifa_concurrence, make_type, msme, cda_paid_amount, unit_paid_amount, recoveries, dv_no, dp_sheet_no, cmp_date, cmp_batch`
- Date column: `created_at`
- Why this source: The bill table is the central transactional record for third-party payments, containing all critical financial, identity, and workflow metadata. It links to DAK and downstream payment tables via FKs, and its validation rules are fully defined in the context. It is the primary source for detecting an

Feature plan:
- `is_bill_with_invoice_date_after_bill_date` (`date_after`): Invoice date must not be after bill creation date. This violates the chronological order of document submission and may indicate backdating.
- `is_bill_with_gst_applicable_without_tax_recovery` (`missing_when_equals`): If GST is applicable, recoveries must be present. Missing recoveries for GST-applicable bills may indicate tax evasion or incomplete payment calculation.
- `is_bill_with_duplicate_normalized_invoice` (`duplicate_normalized_key`): Same invoice identity with different formatting (e.g., case, spaces) across records may indicate attempted evasion of duplicate checks.
- `is_bill_with_high_amount_ratio_to_group_median` (`ratio_to_group_median` by `fk_central_vendor` params={'quantile': 0.95}): Amount passed significantly higher than median for the same vendor may indicate overpayment, unusual billing, or split payment evasion.
- `is_bill_with_low_amount_ratio_to_group_median` (`ratio_to_group_median` by `fk_central_vendor` params={'quantile': 0.05}): Amount passed significantly lower than median for the same vendor may indicate underpayment, bill splitting, or manipulation.
- `is_bill_with_high_amount_rank_pct_in_vendor_group` (`group_rank_pct` by `fk_central_vendor`): A bill with a high percentile rank in amount within its vendor group may indicate outlier behavior or potential overpayment.
- `is_bill_with_low_amount_rank_pct_in_vendor_group` (`group_rank_pct` by `fk_central_vendor` params={'quantile': 0.05}): A bill with a low percentile rank in amount within its vendor group may indicate underpayment or bill splitting.
- `is_bill_with_duplicate_invoice_in_nearby_dates` (`rolling_window_count` params={'window_days': 7}): Same invoice number from same vendor appearing within 7 days suggests possible bill splitting or repeated submission to bypass controls.
- `is_bill_with_high_rolling_window_amount_sum` (`rolling_window_amount_sum` params={'window_days': 7}): High cumulative amount for same invoice/vendor within 7 days may indicate attempted evasion of payment caps or split payment.
- `is_bill_with_no_downstream_payment_trail` (`missing_when_present`): A bill with a valid DAK but no downstream payment records (cheque_slip, schedule3, ecs) suggests a broken payment flow or fake payment attempt.
- `is_bill_with_gst_applicable_and_no_recoveries` (`missing_when_equals`): GST-applicable bills must have recoveries. Missing recoveries may indicate tax evasion or incomplete payment calculation.
- `is_bill_with_duplicate_invoice_in_different_units` (`duplicate_distinct`): Same invoice from same vendor appearing in different units may indicate attempted evasion of unit-level controls or duplicate payment.
- `is_bill_with_duplicate_invoice_in_different_daks` (`duplicate_distinct`): Same invoice from same vendor appearing in different DAKs may indicate duplicate or split payment attempts.
- `is_bill_with_duplicate_invoice_in_different_payment_modes` (`duplicate_distinct`): Same invoice from same vendor in different payment modes may indicate manipulation or split payment to bypass controls.
- `is_bill_with_duplicate_invoice_in_different_dates` (`duplicate_distinct`): Same invoice from same vendor with different dates may indicate backdating or repeated submission.

### cheque_slip

- ID column: `id`
- Amount column: `amount`
- Context columns: `fk_dak, fk_central_vendor, fk_central_beneficiary, fk_central_favour_bank, fk_ecs_payment_mode, payment_detail, approved, record_status, cheque_slip_date, created_at`
- Date column: `cheque_slip_date`
- Why this source: cheque_slip is the final payment stage in the third-party payment flow; it contains the actual payment amount, beneficiary, vendor, bank, payment mode, approval status, and date. It is the most reliable source for detecting anomalies in payment execution, especially when compared to upstream bill an

Feature plan:
- `is_missing_dak_link` (`is_missing`): A missing fk_dak indicates the cheque slip is not linked to any valid DAK, which breaks the payment flow and suggests a fabricated or orphaned payment.
- `is_missing_vendor` (`is_missing`): A missing vendor in a cheque slip implies the payment is made without a valid supplier identity, which is a red flag for fake or unverified payments.
- `is_missing_beneficiary` (`is_missing`): A missing beneficiary indicates the payment lacks a defined recipient, which is suspicious in third-party payments where beneficiary identity is mandatory.
- `is_missing_favour_bank` (`is_missing`): A missing favour bank suggests the payment lacks a defined financial institution, which is critical for traceability and fraud detection.
- `is_missing_payment_mode` (`is_missing`): A missing payment mode indicates the payment type (e.g., ECS, cheque) is undefined, which breaks the payment flow and may indicate manipulation.
- `is_missing_payment_detail` (`is_missing`): A missing payment detail reduces auditability and may indicate a lack of justification for the payment, especially if it should be derived from bill context.
- `is_not_approved` (`equals`): Cheque slips that are not approved should not be processed; this flag detects unapproved payments that may have bypassed control.
- `is_not_valid_record_status` (`equals`): Only record_status 'V' (valid) should be processed; any other status indicates invalid or rejected payment slips.
- `cheque_slip_date_in_future` (`date_in_future`): A future cheque slip date indicates a fabricated or incorrectly dated payment, which violates normal payment chronology.
- `duplicate_vendor_cheque_slip` (`duplicate_key`): Multiple cheque slips for the same vendor may indicate bulk or split payments, which could be used to evade thresholds or detection.
- `duplicate_beneficiary_cheque_slip` (`duplicate_key`): Multiple cheque slips to the same beneficiary may indicate repeated payments or payment splitting.
- `duplicate_payment_mode_cheque_slip` (`duplicate_key`): Multiple cheque slips with the same payment mode may indicate batch processing, but if inconsistent with vendor or amount, may suggest manipulation.
- `duplicate_normalized_payment_detail` (`duplicate_normalized_key`): Normalized duplicate payment details (ignoring case, spaces, punctuation) may indicate repeated or templated payment narratives, which could be used to mask fraud.
- `duplicate_vendor_payment_detail` (`duplicate_distinct`): Same vendor with different payment details may indicate inconsistent or fabricated payment justifications.
- `duplicate_beneficiary_payment_detail` (`duplicate_distinct`): Same beneficiary with different payment details may indicate inconsistent or fabricated payment justifications.
- `rolling_window_vendor_payment_count_7d` (`rolling_window_count` params={'window_days': 7}): High frequency of payments to the same vendor within 7 days may indicate bill splitting or repeated payments to evade scrutiny.
- `rolling_window_vendor_payment_amount_sum_7d` (`rolling_window_amount_sum` params={'window_days': 7}): High total payment amount to the same vendor within 7 days may indicate bill splitting or excessive payments.
- `ratio_to_group_median_vendor_amount` (`ratio_to_group_median` by `fk_central_vendor`): A payment amount significantly higher or lower than the median for the same vendor may indicate overpayment, underpayment, or manipulation.
- `group_rank_pct_vendor_amount` (`group_rank_pct` by `fk_central_vendor`): A payment amount at extreme percentile within the vendor’s history may indicate abnormal behavior.
- `group_frequency_vendor` (`group_frequency` by `fk_central_vendor`): High frequency of payments to a vendor may indicate a pattern of repeated or bulk payments, which could be suspicious if not justified.
- `starts_with_fis_29` (`starts_with`): FIS document numbers starting with '29' indicate GEM online transactions, which are not valid for normal third-party payments and should not be processed as such.

### punching_medium

- ID column: `id`
- Amount column: `amount`
- Context columns: `fk_dak, pm_date, record_status, sign_rc, detail_code_head, category_head, code_head, detail_head, voucher_class, pm_month_year, cancelled, neft_generated, dpsheet_generated, upload_file_generated, cmp_batch, upload_batch, ncs_transaction_id`
- Date column: `pm_date`
- Why this source: punching_medium is a core payment flow table with timestamped, amount-bearing, status-controlled records that link directly to dak. It captures the final accounting event and payment routing context, making it ideal for detecting flow breaks, timing anomalies, and payment trail weaknesses in third-p

Feature plan:
- `is_missing_dak_link` (`is_missing`): A punching medium row without a valid fk_dak indicates a broken upstream link, suggesting a payment record not tied to a legitimate DAK, which is a critical forensic red flag in third-party payment flows.
- `is_missing_pm_date` (`is_missing`): Missing pm_date in a punching medium record breaks the chronological traceability of the payment event, undermining auditability and increasing risk of fraudulent or unverified transactions.
- `is_missing_sign_rc` (`is_missing`): Missing sign_rc prevents aggregation of valid charged amounts; only '+C' rows should be included in payment totals, so missing sign_rc may indicate invalid or unaccounted entries.
- `is_invalid_sign_rc` (`equals`): A sign_rc of '+D' indicates a credit entry, which is invalid in the context of a third-party payment debit. Such entries should not be included in payment flow and may indicate manipulation or reversal.
- `is_neft_generated_but_no_dpsheet` (`missing_when_present`): If NEFT is generated but no DPSheet is created, it suggests a payment was initiated without proper internal documentation, indicating a potential bypass of internal controls.
- `is_dpsheet_generated_but_no_neft` (`missing_when_present`): A DPSheet generated without NEFT indicates a payment was documented but not executed, which may signal a failed or delayed payment, or a potential ghost payment attempt.
- `is_cancelled_with_payment` (`missing_when_equals`): Cancelled punching medium records should not be active (record_status='A'). If a record is active but marked as cancelled, it indicates a status contradiction and potential manipulation of payment state.
- `is_active_with_no_payment_trail` (`missing_when_equals`): Active records (record_status='A') without NEFT generation suggest a payment was approved but not executed, which may indicate a stalled or fraudulent payment process.
- `is_payment_in_future` (`date_in_future`): A future pm_date indicates a payment scheduled for a date in the future, which may be valid, but when combined with active status and no NEFT, it raises suspicion of delayed or fake payment planning.
- `is_payment_with_no_dak` (`missing_when_equals`): Active payment records without a linked DAK are suspicious, as they lack a source document and cannot be traced to a valid claim or approval.
- `is_payment_with_duplicate_dak` (`duplicate_key`): Multiple punching medium records linked to the same DAK may indicate duplicate payment attempts or splitting of a single payment across multiple entries, a common sign of bill splitting or fraud.
- `is_payment_with_duplicate_normalized_cmp_batch` (`duplicate_normalized_key`): Duplicate CMP batch identifiers across different punching medium records may indicate batch-level manipulation or reuse of payment batches, which is a red flag for systemic fraud.
- `is_payment_with_duplicate_normalized_upload_batch` (`duplicate_normalized_key`): Reused upload batches may indicate data reprocessing or duplication, which can mask fraudulent or duplicate payment entries.
- `is_payment_with_duplicate_normalized_ncs_transaction_id` (`duplicate_normalized_key`): Duplicate NCS transaction IDs across different punching medium records suggest possible transaction reuse or manipulation, which is a serious forensic red flag.
- `is_payment_with_duplicate_normalized_detail_code_head` (`duplicate_normalized_key`): Repeated detail_code_head values across different payments may indicate a pattern of reusing accounting codes, which could be used to mask improper or unauthorized expenditures.
- `is_payment_with_duplicate_distinct_cmp_batch` (`duplicate_distinct`): Same CMP batch used across different DAKs may indicate batch-level fraud or data manipulation, especially if the batches are not logically grouped.
- `is_payment_with_duplicate_distinct_upload_batch` (`duplicate_distinct`): Reused upload batches across different DAKs may indicate data reprocessing or duplication, which can hide fraudulent or duplicate payments.
- `is_payment_with_duplicate_distinct_ncs_transaction_id` (`duplicate_distinct`): Same NCS transaction ID used across multiple DAKs is a strong indicator of transaction reuse or manipulation, which is a high-risk forensic signal.
- `is_payment_with_duplicate_distinct_detail_code_head` (`duplicate_distinct`): Same detail_code_head used across different DAKs may indicate a pattern of code reuse, which could be used to mask improper or unauthorized payments.
- `is_payment_with_rolling_window_duplicate_cmp_batch` (`rolling_window_count` params={'window_days': 7}): Repeated CMP batches within a 7-day window may indicate batch-level manipulation or reuse, which is a red flag for systemic fraud.
- `is_payment_with_rolling_window_duplicate_upload_batch` (`rolling_window_count` params={'window_days': 7}): Repeated upload batches within a 7-day window may indicate data reprocessing or duplication, which can mask fraudulent or duplicate payments.
- `is_payment_with_rolling_window_duplicate_ncs_transaction_id` (`rolling_window_count` params={'window_days': 7}): Repeated NCS transaction IDs within a 7-day window suggest transaction reuse or manipulation, which is a serious forensic red flag.
- `is_payment_with_rolling_window_duplicate_detail_code_head` (`rolling_window_count` params={'window_days': 7}): Repeated detail_code_head values within a 7-day window may indicate a pattern of code reuse, which could be used to mask improper or unauthorized expenditures.
- `is_payment_with_rolling_window_amount_sum_cmp_batch` (`rolling_window_amount_sum` params={'window_days': 7}): High rolling sum of amounts for the same CMP batch within 7 days may indicate batch-level manipulation or reuse, which is a red flag for systemic fraud.
- `is_payment_with_rolling_window_amount_sum_upload_batch` (`rolling_window_amount_sum` params={'window_days': 7}): High rolling sum of amounts for the same upload batch within 7 days may indicate data reprocessing or duplication, which can mask fraudulent or duplicate payments.
- `is_payment_with_rolling_window_amount_sum_ncs_transaction_id` (`rolling_window_amount_sum` params={'window_days': 7}): High rolling sum of amounts for the same NCS transaction ID within 7 days suggests transaction reuse or manipulation, which is a serious forensic red flag.
- `is_payment_with_rolling_window_amount_sum_detail_code_head` (`rolling_window_amount_sum` params={'window_days': 7}): High rolling sum of amounts for the same detail_code_head within 7 days may indicate a pattern of code reuse, which could be used to mask improper or unauthorized expenditures.
- `is_payment_with_high_ratio_to_group_median_amount` (`ratio_to_group_median` by `detail_code_head`): A payment amount significantly higher than the median amount for its detail_code_head may indicate an outlier, which could be a sign of overpayment or manipulation.
- `is_payment_with_high_ratio_to_group_median_amount_cmp_batch` (`ratio_to_group_median` by `cmp_batch`): A payment amount significantly higher than the median for its CMP batch may indicate a large outlier, which could be a sign of overpayment or manipulation.
- `is_payment_with_high_ratio_to_group_median_amount_upload_batch` (`ratio_to_group_median` by `upload_batch`): A payment amount much higher than the median for its upload batch may indicate a large outlier, which could be a sign of overpayment or manipulation.
- `is_payment_with_high_ratio_to_group_median_amount_ncs_transaction_id` (`ratio_to_group_median` by `ncs_transaction_id`): A payment amount significantly higher than the median for its NCS transaction ID may indicate a duplicate or inflated transaction, which is a red flag.
- `is_payment_with_high_ratio_to_group_median_amount_detail_code_head` (`ratio_to_group_median` by `detail_code_head`): A payment amount much higher than the median for its detail_code_head may indicate an outlier, which could be a sign of overpayment or manipulation.
- `is_payment_with_high_group_rank_pct_amount` (`group_rank_pct` by `detail_code_head`): A payment amount in the top percentile for its detail_code_head may indicate an outlier, which could be a sign of overpayment or manipulation.
- `is_payment_with_high_group_rank_pct_amount_cmp_batch` (`group_rank_pct` by `cmp_batch`): A payment amount in the top percentile for its CMP batch may indicate a large outlier, which could be a sign of overpayment or manipulation.
- `is_payment_with_high_group_rank_pct_amount_upload_batch` (`group_rank_pct` by `upload_batch`): A payment amount in the top percentile for its upload batch may indicate a large outlier, which could be a sign of overpayment or manipulation.
- `is_payment_with_high_group_rank_pct_amount_ncs_transaction_id` (`group_rank_pct` by `ncs_transaction_id`): A payment amount in the top percentile for its NCS transaction ID may indicate a duplicate or inflated transaction, which is a red flag.
- `is_payment_with_high_group_rank_pct_amount_detail_code_head` (`group_rank_pct` by `detail_code_head`): A payment amount in the top percentile for its detail_code_head may indicate an outlier, which could be a sign of overpayment or manipulation.
- `is_payment_with_high_group_frequency` (`group_frequency` by `detail_code_head`): A detail_code_head used in many payments may indicate a high-volume or high-risk code, which may require closer scrutiny for potential abuse.
- `is_payment_with_high_group_frequency_cmp_batch` (`group_frequency` by `cmp_batch`): A CMP batch used in many payments may indicate batch-level manipulation or reuse, which is a red flag for systemic fraud.
- `is_payment_with_high_group_frequency_upload_batch` (`group_frequency` by `upload_batch`): An upload batch used in many payments may indicate data reprocessing or duplication, which can mask fraudulent or duplicate payments.
- `is_payment_with_high_group_frequency_ncs_transaction_id` (`group_frequency` by `ncs_transaction_id`): An NCS transaction ID used in many payments may indicate transaction reuse or manipulation, which is a serious forensic red flag.
- `is_payment_with_high_group_frequency_detail_code_head` (`group_frequency` by `detail_code_head`): A detail_code_head used in many payments may indicate a high-volume or high-risk code, which may require closer scrutiny for potential abuse.

### schedule3

- ID column: `id`
- Amount column: `schedule3_amount`
- Context columns: `fk_dak, fk_cheque_slip, fk_section, section_code, dp_sheet_date, month, transcription_type, cmp_batch`
- Date column: `dp_sheet_date`
- Why this source: schedule3 is the final payment stage in the third-party payment flow; it contains the last recorded payment amount and date. It links directly to cheque_slip and dak, enabling forensic checks on flow integrity, timing, and downstream consistency. Its high cardinality and direct FK relationships make

Feature plan:
- `schedule3_flow_broken_upstream` (`is_missing`): A missing fk_dak indicates the schedule3 record has no link to the originating DAK, breaking the payment flow and suggesting a fabricated or orphaned payment entry.
- `schedule3_flow_broken_cheque_slip` (`is_missing`): A missing fk_cheque_slip means the schedule3 record is not tied to a valid cheque slip, indicating a payment entry without a verified payment instrument.
- `schedule3_date_in_future` (`date_in_future`): A future dp_sheet_date indicates a payment entry with an invalid or fabricated date, which is a red flag for data manipulation.
- `schedule3_duplicate_dak_payment` (`duplicate_key`): Multiple schedule3 records for the same DAK indicate duplicate payment entries, which may suggest double payment or splitting of a single claim.
- `schedule3_duplicate_cheque_slip_payment` (`duplicate_key`): Multiple schedule3 records linked to the same cheque slip suggest duplicate finalization of a single payment, which is a strong indicator of duplication.
- `schedule3_duplicate_normalized_cmp_batch` (`duplicate_normalized_key`): Normalized CMP batch numbers (case-insensitive, punctuation-stripped) appearing multiple times may indicate batch-level manipulation or reuse of payment batches.
- `schedule3_rolling_window_payment_count` (`rolling_window_count` params={'window_days': 7}): A high count of schedule3 entries for the same DAK within 7 days may indicate bill splitting or rapid-fire payments to evade scrutiny.
- `schedule3_rolling_window_payment_amount_sum` (`rolling_window_amount_sum` params={'window_days': 7}): A large sum of schedule3 amounts for the same DAK within 7 days may indicate a split payment or overpayment attempt.
- `schedule3_amount_ratio_to_dak_median` (`ratio_to_group_median` by `fk_dak`): If the schedule3 amount is significantly higher than the median amount for the same DAK, it may indicate an outlier payment, possibly due to overpayment or manipulation.
- `schedule3_amount_rank_pct` (`group_rank_pct` by `fk_dak`): A very high percentile rank of the schedule3 amount within the same DAK group may indicate an unusually large payment, warranting further review.
- `schedule3_amount_frequency` (`group_frequency` by `fk_dak`): High frequency of the same amount for a single DAK may suggest standardized or templated payments, which could be indicative of fraud or automation abuse.
- `schedule3_transcription_type_invalid` (`equals`): A transcription_type of 'X' is invalid and may indicate a corrupted or unprocessed entry, which could be used to hide fraudulent activity.
- `schedule3_month_invalid` (`is_missing`): Missing month context prevents proper accounting and audit trail alignment, increasing risk of untraceable payments.
- `schedule3_dak_section_invalid` (`missing_when_equals`): If the DAK is in section 142 (TPP), but the schedule3 record has no fk_section, it breaks the business rule and may indicate a misclassified payment.

### ecs

- ID column: `id`
- Amount column: `amount`
- Context columns: `fk_dak, fk_schedule3, fk_cheque_slip, fk_central_beneficiary, fk_bank_pan_detail, fk_ecs_payment_mode, payment_reference_no, cmp_reference_no, scroll_no, utr_no, month, record_status, approved, to_be_paid, released, cmp_payment_status`
- Date column: `cmp_file_gen_date`
- Why this source: ECS is the final payment stage in the third-party payment flow; it contains the last valid payment record with full downstream context. It links to DAK, schedule3, and cheque_slip, enabling forensic checks on flow integrity, approval status, payment timing, and beneficiary alignment. All downstream 

Feature plan:
- `ecs_flow_broken_without_upstream_dak` (`is_missing`): A missing fk_dak in ECS indicates a payment record with no valid DAK origin, violating the mandatory DAK-to-ecs flow. This is a critical forensic red flag for fake or orphaned payments.
- `ecs_flow_broken_without_upstream_schedule3` (`is_missing`): ECS without a linked schedule3 indicates a payment that bypassed the schedule3 stage, which is required for payment validation. This breaks the normal flow and suggests manipulation.
- `ecs_flow_broken_without_upstream_cheque_slip` (`is_missing`): ECS without a cheque_slip link indicates a payment that skipped the cheque slip stage, which is a required intermediate step. This breaks the payment chain and raises suspicion.
- `ecs_payment_with_rejected_status` (`equals`): ECS records with record_status=R (rejected) should not be processed further. Their presence in the final payment stage indicates a flow contradiction and potential manipulation.
- `ecs_payment_with_invalid_payment_status` (`equals`): ECS records with cmp_payment_status=I (invalid) are not eligible for payment. Their presence in the final stage indicates a flow break and potential fraud.
- `ecs_payment_amount_ratio_to_dak_median` (`ratio_to_group_median` by `fk_dak`): ECS payment amount that deviates significantly from the median amount for the same DAK may indicate duplicate or split payment behavior.
- `ecs_payment_amount_ratio_to_schedule3_median` (`ratio_to_group_median` by `fk_schedule3`): ECS payment amount that deviates significantly from the median amount for the same schedule3 may indicate overpayment or manipulation.
- `ecs_payment_amount_ratio_to_cheque_slip_median` (`ratio_to_group_median` by `fk_cheque_slip`): ECS payment amount that deviates significantly from the median amount for the same cheque_slip may indicate overpayment or manipulation.
- `ecs_payment_amount_rank_pct` (`group_rank_pct` by `fk_dak`): ECS payment amount that is an extreme percentile within the same DAK group may indicate outlier behavior or manipulation.
- `ecs_payment_amount_rank_pct_schedule3` (`group_rank_pct` by `fk_schedule3`): ECS payment amount that is an extreme percentile within the same schedule3 group may indicate outlier behavior or manipulation.
- `ecs_payment_amount_rank_pct_cheque_slip` (`group_rank_pct` by `fk_cheque_slip`): ECS payment amount that is an extreme percentile within the same cheque_slip group may indicate outlier behavior or manipulation.
- `ecs_payment_duplicate_dak` (`duplicate_key`): Multiple ECS records for the same DAK may indicate duplicate payment or split payment behavior, which is suspicious in a normal payment flow.
- `ecs_payment_duplicate_schedule3` (`duplicate_key`): Multiple ECS records for the same schedule3 may indicate duplicate payment or split payment behavior, which is suspicious.
- `ecs_payment_duplicate_cheque_slip` (`duplicate_key`): Multiple ECS records for the same cheque_slip may indicate duplicate payment or split payment behavior, which is suspicious.
- `ecs_payment_duplicate_beneficiary` (`duplicate_key`): Multiple ECS records for the same beneficiary may indicate duplicate payment or split payment behavior, which is suspicious.
- `ecs_payment_duplicate_payment_reference` (`duplicate_key`): Multiple ECS records with the same payment_reference_no may indicate duplicate payment or split payment behavior, which is suspicious.
- `ecs_payment_duplicate_cmp_reference` (`duplicate_key`): Multiple ECS records with the same cmp_reference_no may indicate duplicate payment or split payment behavior, which is suspicious.
- `ecs_payment_duplicate_scroll_no` (`duplicate_key`): Multiple ECS records with the same scroll_no may indicate duplicate payment or split payment behavior, which is suspicious.
- `ecs_payment_duplicate_utr_no` (`duplicate_key`): Multiple ECS records with the same utr_no may indicate duplicate payment or split payment behavior, which is suspicious.
- `ecs_payment_duplicate_normalized_utr_no` (`duplicate_normalized_key`): Multiple ECS records with the same normalized utr_no (ignoring case/punctuation) may indicate duplicate payment or split payment behavior, which is suspicious.
- `ecs_payment_duplicate_normalized_cmp_reference` (`duplicate_normalized_key`): Multiple ECS records with the same normalized cmp_reference_no may indicate duplicate payment or split payment behavior, which is suspicious.
- `ecs_payment_duplicate_normalized_payment_reference` (`duplicate_normalized_key`): Multiple ECS records with the same normalized payment_reference_no may indicate duplicate payment or split payment behavior, which is suspicious.
- `ecs_payment_duplicate_normalized_scroll_no` (`duplicate_normalized_key`): Multiple ECS records with the same normalized scroll_no may indicate duplicate payment or split payment behavior, which is suspicious.
- `ecs_payment_duplicate_normalized_beneficiary` (`duplicate_normalized_key`): Multiple ECS records with the same normalized beneficiary may indicate duplicate payment or split payment behavior, which is suspicious.
- `ecs_payment_duplicate_normalized_dak` (`duplicate_normalized_key`): Multiple ECS records with the same normalized DAK may indicate duplicate payment or split payment behavior, which is suspicious.
- `ecs_payment_duplicate_distinct_beneficiary` (`duplicate_distinct`): Multiple ECS records for the same DAK with different beneficiaries may indicate beneficiary diversion or payment to unauthorized parties.
- `ecs_payment_duplicate_distinct_payment_mode` (`duplicate_distinct`): Multiple ECS records for the same DAK with different payment modes may indicate manipulation or inconsistent payment routing.
- `ecs_payment_duplicate_distinct_bank_pan` (`duplicate_distinct`): Multiple ECS records for the same DAK with different bank account details may indicate payment diversion or manipulation.
- `ecs_payment_rolling_window_count_7d_same_dak` (`rolling_window_count` params={'window_days': 7}): High count of ECS payments for the same DAK within 7 days may indicate bill splitting or rapid payment processing, which is suspicious.
- `ecs_payment_rolling_window_amount_sum_7d_same_dak` (`rolling_window_amount_sum` params={'window_days': 7}): High total amount of ECS payments for the same DAK within 7 days may indicate bill splitting or overpayment, which is suspicious.
- `ecs_payment_rolling_window_count_7d_same_beneficiary` (`rolling_window_count` params={'window_days': 7}): High count of ECS payments to the same beneficiary within 7 days may indicate duplicate or split payment behavior, which is suspicious.
- `ecs_payment_rolling_window_amount_sum_7d_same_beneficiary` (`rolling_window_amount_sum` params={'window_days': 7}): High total amount of ECS payments to the same beneficiary within 7 days may indicate duplicate or split payment behavior, which is suspicious.
- `ecs_payment_rolling_window_count_7d_same_payment_reference` (`rolling_window_count` params={'window_days': 7}): High count of ECS payments with the same payment_reference_no within 7 days may indicate duplicate or split payment behavior, which is suspicious.
- `ecs_payment_rolling_window_amount_sum_7d_same_payment_reference` (`rolling_window_amount_sum` params={'window_days': 7}): High total amount of ECS payments with the same payment_reference_no within 7 days may indicate duplicate or split payment behavior, which is suspicious.
- `ecs_payment_rolling_window_count_7d_same_cmp_reference` (`rolling_window_count` params={'window_days': 7}): High count of ECS payments with the same cmp_reference_no within 7 days may indicate duplicate or split payment behavior, which is suspicious.
- `ecs_payment_rolling_window_amount_sum_7d_same_cmp_reference` (`rolling_window_amount_sum` params={'window_days': 7}): High total amount of ECS payments with the same cmp_reference_no within 7 days may indicate duplicate or split payment behavior, which is suspicious.
- `ecs_payment_rolling_window_count_7d_same_scroll_no` (`rolling_window_count` params={'window_days': 7}): High count of ECS payments with the same scroll_no within 7 days may indicate duplicate or split payment behavior, which is suspicious.
- `ecs_payment_rolling_window_amount_sum_7d_same_scroll_no` (`rolling_window_amount_sum` params={'window_days': 7}): High total amount of ECS payments with the same scroll_no within 7 days may indicate duplicate or split payment behavior, which is suspicious.
- `ecs_payment_rolling_window_count_7d_same_utr_no` (`rolling_window_count` params={'window_days': 7}): High count of ECS payments with the same utr_no within 7 days may indicate duplicate or split payment behavior, which is suspicious.
- `ecs_payment_rolling_window_amount_sum_7d_same_utr_no` (`rolling_window_amount_sum` params={'window_days': 7}): High total amount of ECS payments with the same utr_no within 7 days may indicate duplicate or split payment behavior, which is suspicious.
