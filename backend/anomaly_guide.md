# Tulip 2.0 Anomaly Feature Guide

This guide is generated by the configured local LLM from `tulip2.0_schema.md`.
Python validates that every selected table, column, and feature operation exists before writing runtime config.

## LLM Architecture Notes

### dak

The 'dak' table is the entry point of the payment flow. Use its amount, timestamps, and FKs to detect misrouting, delays, and abnormal values. Prioritize features grouped by organizational units (section, unit, central unit) and business attributes (dak type, mode, status) to monitor flow integrity.

### bill

This table is the first stage in the payment flow after dak. Use fk_dak to trace upstream and monitor for missing or delayed transitions. Focus on approved, passed, and record_status to detect wrong routing or unexpected rejections. The amount_claimed is the primary financial measure; use it with group-by on bill_type, unit, and vendor to detect anomalies.

### gem_bill

gem_bill is a critical first-stage bill table in the payment flow. Use fk_dak to trace back to the originating dak. Monitor approval, payment status, and amount anomalies across units, sections, and vendors. Watch for delayed processing (age_days), unusual timing (hour), and abnormal approval ratios.

### cheque_slip

This table is a key stage in the payment flow. Focus on detecting anomalies in approval patterns, timing delays, and amount deviations relative to the originating dak. Use FKs like fk_dak, fk_unit, and fk_central_unit to trace flow integrity.

### punching_medium

This table is a critical stage in the payment flow. Use `fk_dak` to trace back to the original dak, and monitor `approved`, `record_status`, and `pm_date` for anomalies in routing and timing. Focus on `amount` and `fk_dak` to detect missing or duplicated entries in the flow.

### schedule3

This table is a key stage in the payment flow. Use `fk_dak`, `fk_cheque_slip`, and `approved` to trace flow integrity. Monitor `schedule3_amount` and `record_status` for anomalies in value and state transitions.

### ecs

This table represents the final stage of the payment flow. Focus on detecting anomalies in payment amounts, approval patterns, and timing. Use fk_dak, fk_schedule3, and fk_cheque_slip to trace back to earlier stages for root-cause analysis. Monitor approved status and amount distribution across offices and months.

### cash_requisition

This table is a primary entry point in the payment flow. Focus on detecting anomalies in approval patterns, amount outliers, and delays. Use fk_dak to trace back to the originating dak and ensure flow continuity. Monitor approved status and section-level behavior for irregularities.

### gem_product

This table links to gem_bill and consignment_detail via FKs. Use product_code, product_name, and sub_category to detect anomalies in product-level transactions. Monitor total_value and quantity_ordered for outliers. Track expected_delivery_date for delays affecting downstream flow.

### civ_medical_bill

This table is a critical first-stage bill table in the payment flow. Focus on detecting wrong routing (via fk_dak), missing stages (via passed/approved status), and unusual acceptance patterns. Use fk_dak, fk_bill_type, and status columns to trace flow integrity.

### civ_paybill

This table is a critical stage in the payment flow. Use fk_dak to trace back to the original dak. Monitor passed status and audit checks to detect improper routing or acceptance. The amount_claimed column is the primary financial measure. Use created_at for time-based anomaly detection.

### civ_tada_ltc_bill

This table is a critical stage in the TADA/LTC payment flow. Focus on detecting misrouting, stalled approvals, and abnormal claim amounts. Use FKs to dak, unit, and bill_type to trace flow integrity. Monitor approval and pass status per dak to detect anomalies in workflow progression.

### echs_medical_bill

This table is a key first-stage bill table in the ECHS medical payment flow. Use `fk_dak` to trace back to the originating dak. Monitor `approved`, `record_status`, and `amount_claimed` for anomalies. Track movement through `fk_bpa_vendor`, `fk_unit`, and `fk_central_unit` to detect routing errors or unexpected processing paths.

### cmp_scroll

This table is a key staging point in the payment flow. Monitor for stalled records (long scroll_age_days), abnormal status distributions, and outliers in amount by approval, payment mode, or CDA/sub-office. Use fk_ecs and fk_usr to trace flow progression and detect missing or incorrect downstream links.

### civ_employee_earning

This table links employee earnings to paybills and central entities. Use `fk_civ_paybill` to trace flow from paybill to cheque slip and beyond. Monitor `amount` and `record_status` for anomalies in routing and acceptance. `created_at` helps detect delays in processing.

### civ_employee_recovery

This table is a downstream stage in the payment flow, linked to `civ_paybill`. Anomalies in amount, timing, or status may indicate incorrect recovery processing, misrouting, or stalled workflows. Prioritize features that detect deviations by pay code, office, central unit, and month to identify systemic issues.

### civ_emp_allowances

This table tracks employee allowances and is critical for detecting anomalies in approval, amount, and flow. Focus on approval status, employee and office context, and creation timing. Use `fk_central_civ_employee` and `fk_office_id` to trace flow and detect misrouting. Watch for outliers in amount per employee or office, and unusual creation times.

### recoveries

This table tracks recovery actions post-approval. Use `fk_dak` to trace back to the original dak. Monitor `amount` anomalies per `fk_office_id`, `fk_vendor`, and `recovery_rule`. `created_at` helps detect delays. `recovery_month` enables time-based anomaly detection.

## Runtime Sources And Feature Plans

### dak

- ID column: `id`
- Amount column: `amount`
- Context columns: `fk_section, fk_dak_type, fk_unit, fk_central_unit, mode_of_receipt, kpi_bill_type, record_status`
- Date column: `created_at`
- Why this source: The 'dak' table is the root of the payment flow, with key metadata for tracking routing, status, and financial value. It contains a reliable amount and timestamp, and multiple FKs that link to organizational units and workflow stages.

Feature plan:
- `dak_age_days` (`age_days` from `event_date`): Measures how long each dak has been in the system since creation, helping detect delays or stuck records in the payment flow.
- `dak_amount_ratio_to_median` (`ratio_to_median` from `amount`): Identifies daks with unusually high or low amounts compared to the median, which may indicate misrouting, fraud, or data entry errors.
- `dak_amount_ratio_to_group_median_by_section` (`ratio_to_group_median` by `fk_section`): Detects abnormal amount patterns within specific sections, which could signal inconsistent processing or anomalies in unit-level workflows.
- `dak_amount_ratio_to_group_median_by_dak_type` (`ratio_to_group_median` by `fk_dak_type`): Highlights outliers in amount distribution by dak type, useful for identifying misclassified or incorrectly routed daks.
- `dak_amount_ratio_to_group_median_by_unit` (`ratio_to_group_median` by `fk_unit`): Reveals anomalies in amount size per unit, which may indicate incorrect assignment or irregular processing behavior.
- `dak_amount_ratio_to_group_median_by_central_unit` (`ratio_to_group_median` by `fk_central_unit`): Flags unusual amounts tied to central units, potentially indicating misrouting or systemic issues in high-level processing.
- `dak_amount_ratio_to_group_median_by_mode_of_receipt` (`ratio_to_group_median` by `mode_of_receipt`): Detects abnormal amount distributions by receipt mode, which could indicate improper handling or fraud in specific channels.
- `dak_amount_ratio_to_group_median_by_kpi_bill_type` (`ratio_to_group_median` by `kpi_bill_type`): Identifies anomalies in amount patterns by bill type, critical for detecting wrong routing or incorrect classification in the flow.
- `dak_amount_ratio_to_group_median_by_record_status` (`ratio_to_group_median` by `record_status`): Highlights abnormal amounts in records with specific statuses, which may indicate premature closure, rejection, or incorrect status assignment.
- `dak_created_hour` (`hour` from `event_date`): Captures time-of-day patterns in dak creation, useful for detecting unusual activity outside normal business hours.
- `dak_created_day_of_week` (`day_of_week` from `event_date`): Reveals day-of-week trends in dak creation, helping detect irregular spikes or drops that may indicate process disruption.

### bill

- ID column: `id`
- Amount column: `amount_claimed`
- Context columns: `fk_dak, fk_section, fk_unit, fk_vendor, fk_bill_type, approved, passed, record_status, payment_mode, month`
- Date column: `created_at`
- Why this source: The bill table is a core flow table in the payment workflow, serving as the first stage after dak. It contains key financial and status indicators, with strong FK relationships to upstream and downstream tables. The amount_claimed and created_at are reliable, non-null measures for anomaly detection.

Feature plan:
- `amount_claimed_ratio_to_group_median_by_bill_type` (`ratio_to_group_median` by `fk_bill_type`): Detects outliers in amount claimed within specific bill types, helping identify misclassification or abnormal spending patterns.
- `amount_claimed_ratio_to_group_median_by_unit` (`ratio_to_group_median` by `fk_unit`): Highlights anomalies in unit-level spending, useful for detecting irregularities in specific units' billing behavior.
- `amount_claimed_ratio_to_group_median_by_vendor` (`ratio_to_group_median` by `fk_vendor`): Flags unusual billing amounts from specific vendors, which may indicate vendor fraud or incorrect invoicing.
- `approved_ratio_to_group_frequency` (`group_frequency` by `fk_bill_type`): Measures the frequency of approved bills per bill type; low frequency may indicate bottlenecks or rejection issues.
- `passed_ratio_to_group_frequency` (`group_frequency` by `fk_bill_type`): Tracks how often bills are passed per type; low pass rates may indicate systemic rejection or verification issues.

### gem_bill

- ID column: `id`
- Amount column: `bill_amount`
- Context columns: `fk_dak, fk_section, fk_unit, fk_office_id, fk_central_unit, vendor_name, payment_status, approved, record_status, make_type`
- Date column: `bill_date`
- Why this source: gem_bill is a first-stage bill table in the payment flow, directly linked to dak via fk_dak. It contains critical financial and status metadata for anomaly detection in routing, approval, and payment progression.

Feature plan:
- `bill_amount_ratio_to_group_median_by_vendor` (`ratio_to_group_median` by `vendor_name`): Detects outliers in bill amounts per vendor; sudden spikes may indicate duplicate or fraudulent invoices from a single vendor.
- `bill_amount_ratio_to_group_median_by_section` (`ratio_to_group_median` by `fk_section`): Highlights abnormal spending patterns within a section; deviations may suggest misrouting or unauthorized approvals.
- `bill_amount_ratio_to_group_median_by_unit` (`ratio_to_group_median` by `fk_unit`): Flags unusual bill values per unit, which could indicate incorrect assignment or anomalies in procurement behavior.
- `bill_amount_ratio_to_group_median_by_office` (`ratio_to_group_median` by `fk_office_id`): Reveals anomalies in bill amounts by office, useful for detecting regional inconsistencies or misrouting in the payment workflow.
- `bill_amount_ratio_to_group_median_by_central_unit` (`ratio_to_group_median` by `fk_central_unit`): Monitors spending patterns across central units; deviations may indicate improper allocation or bypassing of approval stages.
- `bill_approved_ratio_to_group_median` (`ratio_to_group_median` by `fk_section`): Detects sections with abnormally low or high approval rates, which may indicate systemic issues in review processes or bypassing of checks.
- `bill_payment_status_frequency` (`group_frequency` by `payment_status`): Tracks the frequency of each payment status; sudden drops in 'approved' or 'paid' status may indicate bottlenecks or process failures.
- `bill_age_days` (`age_days` from `event_date`): Measures how long a bill has been pending; long delays may indicate stalled processing or missing approvals.

### cheque_slip

- ID column: `id`
- Amount column: `amount`
- Context columns: `approved, record_status, fk_dak, fk_unit, fk_central_unit, fk_favour_bank, fk_office_id`
- Date column: `cheque_slip_date`
- Why this source: The cheque_slip table is a critical flow table in the payment workflow, directly linked to dak and subsequent stages. It contains monetary amounts, approval status, and key FKs that track routing and processing. The date column 'cheque_slip_date' is essential for timing analysis, and the presence of

Feature plan:
- `cheque_slip_approval_ratio` (`ratio_to_group_median` by `approved`): Detects if approved cheque slips have significantly different average amounts than unapproved ones, which may indicate incorrect routing or manipulation.
- `cheque_slip_status_frequency` (`group_frequency` by `record_status`): Identifies unusual frequency of certain record statuses (e.g., 'R' or 'D') that may indicate processing bottlenecks or systemic rejection.
- `cheque_slip_amount_ratio_to_dak_median` (`ratio_to_group_median` by `fk_dak`): Compares cheque slip amount to the median amount of its associated dak; large deviations may indicate incorrect or fraudulent linking.
- `cheque_slip_unit_approval_rate` (`group_rank_pct` by `fk_unit`): Ranks approval rates by unit to detect units with unusually high or low acceptance, which may signal policy violations or misrouting.

### punching_medium

- ID column: `id`
- Amount column: `amount`
- Context columns: `fk_dak, fk_section, fk_unit, fk_office_id, fk_central_unit, approved, record_status, pm_section, cda_code, voucher_class, pm_month_year, category_head, code_head, detail_head, sign_rc`
- Date column: `pm_date`
- Why this source: punching_medium is a key flow table in the payment process, directly linked to dak and later stages. It contains monetary amounts, timestamps, and critical FKs for tracking routing and approval status across organizational units.

Feature plan:
- `punching_medium_age_days` (`age_days` from `event_date`): measures how long each punching_medium record has been pending since its pm_date, helping detect delays in processing that may indicate bottlenecks or stalled flows.
- `punching_medium_amount_ratio_to_median` (`ratio_to_median` from `amount`): identifies unusually large or small amounts relative to the overall median, which may indicate errors in data entry, fraud, or misrouting in the payment flow.
- `punching_medium_approved_ratio_to_group_median` (`ratio_to_group_median` by `approved`): compares amount distribution between approved and unapproved records; deviations may indicate inconsistent approval patterns or anomalies in acceptance thresholds.
- `punching_medium_record_status_frequency` (`group_frequency` by `record_status`): measures the log frequency of records by status; sudden spikes or drops in status counts may indicate system issues or abnormal workflow behavior.
- `punching_medium_fk_dak_frequency` (`group_frequency` by `fk_dak`): identifies dak IDs with unusually high or low numbers of punching_medium records, which may indicate missing or duplicated entries in the flow.
- `punching_medium_voucher_class_ratio_to_group_median` (`ratio_to_group_median` by `voucher_class`): detects abnormal amount distributions across voucher classes, which could signal misclassification or incorrect routing in the payment system.

### schedule3

- ID column: `id`
- Amount column: `schedule3_amount`
- Context columns: `approved, record_status, fk_section, fk_office_id, transcription_type, month`
- Date column: `created_at`
- Why this source: schedule3 is a critical flow table in the payment pipeline, directly linked to dak and cheque_slip via FKs. It contains monetary amounts and status indicators essential for detecting anomalies in routing, approval, and flow progression.

Feature plan:
- `schedule3_approved_ratio_to_group_median` (`ratio_to_group_median` by `transcription_type`): Identifies abnormal approval rates within transcription types, signaling potential workflow breakdowns or inconsistent processing patterns.
- `schedule3_record_status_frequency` (`group_frequency` by `record_status`): Highlights rare or unexpected record_status values, which may indicate data corruption or non-compliant state transitions in the payment flow.

### ecs

- ID column: `id`
- Amount column: `amount`
- Context columns: `record_status, approved, fk_dak, fk_schedule3, fk_cheque_slip, fk_office_id, fk_auditor, fk_aao, month`
- Date column: `created_at`
- Why this source: The ecs table is a final stage in the payment flow, and its columns reflect key business states (approved, record_status), relationships to prior stages (fk_dak, fk_schedule3, fk_cheque_slip), and operational context (office, auditor, aao, month). The created_at timestamp enables time-based anomaly 

Feature plan:
- `ratio_to_median_amount` (`ratio_to_median` from `amount`): Detects unusually high or low payment amounts compared to the overall median, which may indicate fraudulent or erroneous entries in the final payment stage.
- `ratio_to_group_median_amount_by_office` (`ratio_to_group_median` by `fk_office_id`): Identifies outliers in payment amounts within specific offices, which could signal inconsistent processing or manipulation at the office level.
- `group_frequency_approved_by_month` (`group_frequency` by `month`): Measures the frequency of approved payments per month to detect sudden drops or spikes, which may indicate workflow disruptions or data entry issues.
- `ratio_to_quantile_amount_95` (`ratio_to_quantile` from `amount` params={'quantile': 0.95}): Flags payments significantly larger than the 95th percentile, which may represent high-risk or anomalous transactions in the final payment stage.
- `group_rank_pct_approved` (`group_rank_pct` by `approved`): Compares payment amounts within approved vs. unapproved groups to detect if rejected records have unusually high values, suggesting potential rejection bias or manipulation.

### cash_requisition

- ID column: `id`
- Amount column: `amount`
- Context columns: `fk_dak, fk_section, fk_unit, fk_office_id, fk_central_unit, approved, record_status, month`
- Date column: `created_at`
- Why this source: The cash_requisition table is a key first-stage bill table in the payment flow, directly linked to dak via fk_dak. It contains monetary amounts, approval status, and organizational context, making it critical for detecting anomalies in routing, approval patterns, and flow progression.

Feature plan:
- `amount_ratio_to_median` (`ratio_to_median` from `amount`): Identifies unusually high or low amounts compared to the overall median, which may indicate fraudulent or erroneous entries in the cash requisition process.
- `approved_ratio_to_group_median` (`ratio_to_group_median` by `approved`): Compares the amount of approved vs. unapproved requisitions to detect if approved requests are systematically larger or smaller than expected, which could signal approval bypass or rejection bias.
- `month_frequency_log` (`group_frequency` by `month`): Measures the log frequency of requisitions per month to detect abnormal spikes or drops in activity, which may indicate process disruption or data entry errors.
- `fk_section_rank_pct` (`group_rank_pct` by `fk_section`): Ranks the amount within each section to detect unusually high or low values per section, which may indicate misrouting or inconsistent handling across units.

### gem_product

- ID column: `id`
- Amount column: `total_value`
- Context columns: `product_code, product_name, product_brand, quantity_unit_type, offering_type, sub_category`
- Date column: `expected_delivery_date`
- Why this source: The gem_product table contains detailed product-level transaction data with a clear monetary measure (total_value) and key contextual attributes for grouping. It is linked to gem_bill and consignment_detail, making it a critical node in the payment flow from dak to bill to consignment.

Feature plan:
- `ratio_to_group_median_quantity_ordered` (`ratio_to_group_median` by `product_name`): Identifies abnormal order quantities for a product, which could signal incorrect procurement, fraud, or misrouting in the supply chain.
- `group_frequency_product_brand` (`group_frequency` by `product_brand`): Highlights product brands with unusually high or low transaction frequency, which may indicate anomalies in supplier selection or procurement patterns.

### civ_medical_bill

- ID column: `id`
- Amount column: `amount_claimed`
- Context columns: `fk_dak, fk_bill_type, fk_bill_nature, fk_unit, fk_central_unit, fk_section, approved, passed, record_status, claim_type, employee_category, inpatient_outpatient`
- Date column: `created_at`
- Why this source: This table is a key first-stage bill table in the payment flow, directly linked to dak via fk_dak. It contains critical financial and status metadata for anomaly detection in routing, approval, and flow progression.

Feature plan:
- `amount_claimed_ratio_to_group_median_by_bill_type` (`ratio_to_group_median` by `fk_bill_type`): Detects outliers in claim amounts within specific bill types, helping identify misclassification or abnormal billing patterns.
- `amount_claimed_ratio_to_group_median_by_unit` (`ratio_to_group_median` by `fk_unit`): Highlights anomalies in claims from specific units, which may suggest systemic issues or misuse.
- `amount_claimed_ratio_to_group_median_by_central_unit` (`ratio_to_group_median` by `fk_central_unit`): Reveals unusual claim sizes within central units, useful for detecting anomalies in centralized processing.
- `claim_passed_ratio_to_group_frequency` (`group_frequency` by `fk_bill_type`): Measures the frequency of passed claims per bill type, helping detect abnormal rejection or acceptance patterns.
- `claim_approved_ratio_to_group_frequency` (`group_frequency` by `fk_bill_type`): Tracks approval frequency by bill type to detect deviations from expected approval rates.

### civ_paybill

- ID column: `id`
- Amount column: `amount_claimed`
- Context columns: `fk_dak, fk_section, fk_unit, fk_bill_type, record_status, passed, verified_audit_checks`
- Date column: `created_at`
- Why this source: The civ_paybill table is a key stage in the payment flow, directly linked to dak via fk_dak. It contains financial amounts (amount_claimed), status indicators (passed, record_status), and critical FKs for tracking flow progression. The created_at timestamp enables time-based anomaly detection.

Feature plan:
- `passed_ratio_to_group_median` (`ratio_to_group_median` by `passed`): Compares claim amounts between passed and non-passed records; unusually high ratios in passed records may suggest improper approval.
- `passed_frequency_log` (`group_frequency` by `passed`): Measures how frequently passed vs. non-passed records appear; sudden drops in passed frequency may indicate workflow disruption.
- `verified_audit_checks_rank_pct` (`group_rank_pct` by `verified_audit_checks`): Ranks claim amounts within verified and non-verified groups; high-ranked claims in non-verified groups may indicate bypassed checks.

### civ_tada_ltc_bill

- ID column: `id`
- Amount column: `amount_claimed`
- Context columns: `fk_dak, fk_section, fk_office_id, fk_central_unit, fk_unit, fk_civ_employee, fk_bill_type, approved, passed, record_status, payment_record_type, employee_type, month, stage`
- Date column: `created_at`
- Why this source: This table is a key stage in the payment flow (dak -> civ_tada_ltc_bill -> cheque_slip), with strong FK relationships to upstream and downstream tables. It contains critical financial and status metadata for anomaly detection in routing, approval, and flow progression.

Feature plan:
- `claim_amount_ratio_to_group_median_by_unit` (`ratio_to_group_median` by `fk_unit`): Identifies outliers in claim amounts within specific units, helping detect anomalies in local claim patterns that may suggest misrouting or inconsistent approval standards.
- `claim_amount_ratio_to_group_median_by_employee_type` (`ratio_to_group_median` by `employee_type`): Highlights abnormal claim sizes for specific employee types, which could indicate incorrect classification or policy violations in the TADA/LTC flow.
- `claim_amount_ratio_to_group_median_by_bill_type` (`ratio_to_group_median` by `fk_bill_type`): Reveals anomalies in claim amounts by bill type, which is critical for detecting misclassification or incorrect routing in the payment workflow.
- `claim_approved_ratio` (`group_frequency` by `fk_dak`): Tracks the frequency of approved claims per dak, helping detect cases where a dak has multiple claims but none approved, suggesting possible rejection or routing failure.
- `claim_passed_ratio` (`group_frequency` by `fk_dak`): Monitors the proportion of claims passed per dak, identifying daks with unusually low or high pass rates that may indicate systemic issues in approval or rejection patterns.
- `claim_stage_rank_pct` (`group_rank_pct` by `fk_dak`): Ranks claim stages within each dak to detect claims that are stuck at low stages or jump ahead unexpectedly, indicating flow disruption or incorrect progression.

### echs_medical_bill

- ID column: `id`
- Amount column: `amount_claimed`
- Context columns: `fk_dak, fk_section, fk_unit, fk_bpa_vendor, fk_vendor, fk_central_unit, fk_central_beneficiary, approved, record_status, payee_category, ip_op_emer`
- Date column: `claim_date`
- Why this source: This table represents the first stage of the ECHS medical payment flow after the dak. It contains critical financial and routing metadata, including claim amounts, approval status, and FKs to upstream entities like dak and vendor, enabling detection of routing anomalies and flow deviations.

Feature plan:
- `claim_amount_ratio_to_group_median_by_vendor` (`ratio_to_group_median` by `fk_bpa_vendor`): Detects outliers in claim amounts per vendor, helping identify suspicious or inconsistent billing patterns from specific vendors.
- `claim_amount_ratio_to_group_median_by_unit` (`ratio_to_group_median` by `fk_unit`): Highlights abnormal claim sizes within specific units, which may indicate misrouting, incorrect approvals, or systemic issues in unit-level processing.
- `claim_amount_ratio_to_group_median_by_central_unit` (`ratio_to_group_median` by `fk_central_unit`): Reveals anomalies in claim amounts by central unit, useful for detecting regional or organizational-level irregularities in claim processing.
- `claim_amount_ratio_to_group_median_by_status` (`ratio_to_group_median` by `record_status`): Compares claim amounts across different record statuses (e.g., pending, approved, rejected), flagging unexpected financial patterns in rejected or pending claims.
- `claim_amount_ratio_to_group_median_by_approved` (`ratio_to_group_median` by `approved`): Identifies unusually large or small claims among approved vs. unapproved records, which may indicate incorrect approval decisions or data inconsistencies.
- `claim_frequency_by_section` (`group_frequency` by `fk_section`): Measures the volume of claims per section, identifying sections with abnormally high or low activity, which may indicate misrouting or underutilization.

### cmp_scroll

- ID column: `id`
- Amount column: `amount`
- Context columns: `payment_status, payment_mode, cda_code, sub_office_code, approved, record_status`
- Date column: `scroll_date`
- Why this source: The cmp_scroll table is a central staging table in the payment flow, capturing scroll-level details with key business attributes like payment_status, payment_mode, and approval state. It links to downstream tables via fk_ecs and fk_usr, making it critical for detecting anomalies in routing and flow 

Feature plan:
- `scroll_age_days` (`age_days` from `event_date`): Measures how long a record has remained in the scroll stage; unusually long durations may indicate stalled processing or incorrect routing.
- `scroll_payment_status_frequency` (`group_frequency` by `payment_status`): Identifies how frequently each payment status occurs; sudden drops in expected statuses (e.g., 'APPROVED') may indicate systemic rejection or bypassing.
- `scroll_payment_mode_ratio_to_median` (`ratio_to_median` from `amount`): Compares individual scroll amounts to the median amount; outliers may indicate incorrect payment mode assignments or fraudulent activity.
- `scroll_approved_ratio_to_group_median` (`ratio_to_group_median` by `approved`): Compares amounts by approval status; unusually high ratios for unapproved records may suggest improper processing or data leakage.
- `scroll_cda_code_rank_pct` (`group_rank_pct` by `cda_code`): Ranks scroll amounts within each CDA code group; extreme ranks may indicate misrouting or incorrect allocation of funds.
- `scroll_sub_office_code_ratio_to_quantile` (`ratio_to_quantile` from `amount` params={'quantile': 0.95}): Flags amounts that exceed the 95th percentile within their sub-office group; may indicate unusual or unauthorized payments.

### civ_employee_earning

- ID column: `id`
- Amount column: `amount`
- Context columns: `fk_civ_paybill, fk_central_unit, fk_central_civ_employee, fk_office_id, record_status, month, code_head`
- Date column: `created_at`
- Why this source: This table contains employee earning records linked to paybills and central entities, with a clear monetary amount and timestamp. It is a key node in the payment flow, especially for tracking movement from paybill to later stages.

Feature plan:
- `earning_amount_ratio_to_median` (`ratio_to_median` from `amount`): Detects unusually high or low earnings within a specific paybill, which may indicate incorrect routing, duplicate entries, or data entry errors in the payment flow.
- `earning_amount_ratio_to_group_median` (`ratio_to_group_median` by `fk_central_unit`): Identifies outliers in earnings per central unit, which could signal anomalies in payroll processing or misclassification of employees within units.
- `earning_amount_rank_pct` (`rank_pct` from `amount`): Measures how an individual employee's earning compares within their own record history; sudden spikes may indicate incorrect or fraudulent entries.
- `earning_month_frequency` (`group_frequency` by `month`): Monitors the volume of earnings per month; sudden drops or spikes may indicate missing data, system issues, or irregular processing patterns.
- `earning_record_status_ratio` (`group_frequency` by `record_status`): Tracks the distribution of record statuses; unexpected changes in status frequency may indicate incorrect approval or rejection patterns.

### civ_employee_recovery

- ID column: `id`
- Amount column: `amount`
- Context columns: `record_status, fk_civ_paybill, fk_pay_code, fk_office_id, fk_central_unit, fk_central_civ_employee, month, code_head`
- Date column: `created_at`
- Why this source: This table tracks employee recovery records linked to paybills and central employee data; it is part of the payment flow after the initial bill stage. The presence of FKs to paybill and central entities enables flow tracking. The amount and created_at are critical for anomaly detection in timing and

Feature plan:
- `amount_ratio_to_median` (`ratio_to_median` from `amount`): Detects unusually high or low recovery amounts compared to the overall median, which may indicate incorrect recovery values or fraud.
- `amount_ratio_to_group_median_by_pay_code` (`ratio_to_group_median` by `fk_pay_code`): Identifies recovery amounts that deviate significantly from the typical amount for a given pay code, which may indicate misclassification or incorrect processing.
- `amount_ratio_to_group_median_by_central_unit` (`ratio_to_group_median` by `fk_central_unit`): Highlights anomalies in recovery amounts per central unit, which could signal inconsistent application of recovery rules or data entry errors.
- `amount_ratio_to_group_median_by_office` (`ratio_to_group_median` by `fk_office_id`): Reveals outliers in recovery amounts by office, potentially indicating local policy deviations or incorrect approvals.
- `amount_ratio_to_group_median_by_month` (`ratio_to_group_median` by `month`): Detects abnormal recovery volumes in specific months, which may reflect seasonal anomalies or data processing errors.
- `created_at_age_days` (`age_days` from `event_date`): Measures how long a recovery record has been pending since creation; long delays may indicate bottlenecks or stalled processing.
- `created_at_hour` (`hour` from `event_date`): Identifies unusual timing of recovery record creation (e.g., late-night entries), which may suggest automated or unauthorized entries.
- `created_at_day_of_week` (`day_of_week` from `event_date`): Detects non-standard creation days (e.g., weekends), which may indicate off-cycle processing or system errors.
- `record_status_frequency` (`group_frequency` by `record_status`): Measures the relative frequency of different record statuses; sudden shifts may indicate processing issues or systemic rejection patterns.

### civ_emp_allowances

- ID column: `id`
- Amount column: `amount`
- Context columns: `record_status, approved, fk_central_civ_employee, fk_office_id, fk_pay_code, code_head`
- Date column: `created_date`
- Why this source: This table contains employee allowance records with financial amounts, approval status, and key identifiers for tracking flow through payroll and payment systems. It is a critical node in the payment flow where anomalies in approval, status, or amount can indicate routing errors or invalid entries.

Feature plan:
- `amount_ratio_to_median` (`ratio_to_median` from `amount`): Detects unusually high or low allowance amounts compared to the overall median, which may indicate incorrect entries or fraudulent claims.
- `approved_ratio_to_group_median` (`ratio_to_group_median` by `approved`): Compares allowance amounts by approval status to detect if approved records have significantly different values than unapproved ones, which could indicate improper approval patterns.
- `record_status_group_frequency` (`group_frequency` by `record_status`): Measures the log frequency of records per status to detect abnormal spikes or drops in record counts, which may indicate data corruption or missing stages in the workflow.
- `fk_central_civ_employee_group_rank_pct` (`group_rank_pct` by `fk_central_civ_employee`): Ranks allowance amounts within each employee to detect outliers in individual compensation, which may indicate incorrect or duplicate entries.
- `fk_office_id_group_rank_pct` (`group_rank_pct` by `fk_office_id`): Ranks allowance amounts by office to detect abnormal spending patterns per office, which may indicate misrouting or policy violations.

### recoveries

- ID column: `id`
- Amount column: `amount`
- Context columns: `fk_dak, fk_recovery_code, fk_office_id, fk_central_vendor, fk_vendor, record_status, recovery_month, recovery_rule`
- Date column: `created_at`
- Why this source: The recoveries table tracks post-approval recovery actions tied to daks, with strong FK relationships to key workflow tables. It contains a numeric amount, a creation timestamp, and multiple context columns that enable tracking of recovery patterns across offices, vendors, and recovery rules.

Feature plan:
- `recovery_amount_ratio_to_median` (`ratio_to_median` from `amount`): Identifies recoveries with unusually high or low amounts compared to the overall median, which may indicate incorrect recovery values or fraud.
- `recovery_amount_ratio_to_group_median_by_office` (`ratio_to_group_median` by `fk_office_id`): Detects outliers in recovery amounts per office, helping identify abnormal recovery behavior in specific offices.
- `recovery_amount_ratio_to_group_median_by_vendor` (`ratio_to_group_median` by `fk_vendor`): Highlights recoveries with extreme amounts for specific vendors, which may suggest incorrect vendor assignments or anomalies in vendor recovery patterns.
- `recovery_amount_ratio_to_group_median_by_recovery_rule` (`ratio_to_group_median` by `recovery_rule`): Reveals anomalies in recovery amounts grouped by recovery rule, which can flag misapplied rules or incorrect recovery logic.
- `recovery_frequency_by_month` (`group_frequency` by `recovery_month`): Captures the log frequency of recoveries per month, enabling detection of unusual spikes or drops in recovery activity that may indicate process failures or data issues.
- `recovery_rank_pct_by_office` (`group_rank_pct` by `fk_office_id`): Ranks recovery amounts within each office to detect unusually high or low recoveries, which may indicate misrouting or incorrect approvals.
