# Tulip 2.0 Anomaly Feature Guide

This guide is generated by the configured local LLM from `tulip2.0_schema.md`.
Python validates that every selected table, column, and feature operation exists before writing runtime config.

## Validation Context Files

- `/home/ruthikreddy/Documents/validation_files/THIRD_PARTY_PAYMENTS_VALIDATION.md`

## LLM Architecture Notes

### dak

This source plan focuses on the root DAK table for THIRD_PARTY_PAYMENTS (sections 142, 228, 265, 383). It prioritizes forensic identity checks (dakid_no, bill_no, reference_no), date chronology (list_date, bill_date, reference_date), duplicate and normalized duplicate detection, and group-based amount anomalies. Features are designed to detect invoice reuse, splitting, fake identities, abnormal timing, and status contradictions. All features are deterministic, numeric, safe for nulls, and explainable to auditors. The plan leverages validation_context to enforce business rules like FIS prefix 29, gem_bill flag, and required fields.

### bill

This feature plan is designed for the `bill` table as the primary source for detecting anomalies in third-party payments. It leverages all available forensic signals: identity, approval, flow, amount, date, and context. Features are prioritized by risk: broken flow, skipped stages, approval contradictions, amount inconsistencies, duplicate invoice patterns, and abnormal repetition. All features are deterministic, numeric, safe for nulls, and explainable to auditors. The plan uses only columns from `full_usable_columns` and respects `relationship_context` and `validation_context` to ensure business validity.

### cheque_slip

This source plan focuses on the final payment stage (cheque_slip) in the third-party payment flow. It detects anomalies in payment routing, approval, timing, vendor/beneficiary consistency, amount integrity, and flow breaks. All features are deterministic, numeric, and explainable to auditors. The plan leverages FK relationships to DAK for upstream validation and prioritizes flow, timing, and identity contradictions.

### punching_medium

This source plan focuses on detecting anomalies in the third-party payment flow at the punching_medium level. It prioritizes flow breaks, approval contradictions, duplicate payments, and timing violations. All features are deterministic, numeric, and grounded in visible columns. The plan leverages FK relationships to dak for upstream validation and uses rolling windows to detect bill splitting. No LLM reasoning is required at runtime.

### schedule3

schedule3 is the final stage in the third-party payment flow. It must be linked to a valid, approved cheque slip and a valid DAK. Any missing or inconsistent linkage, amount mismatch, or date violation is a forensic red flag. Focus on broken flow, duplicate payments, and final payment anomalies.

### ecs

ECS is the final stage in the third-party payment flow. This source plan focuses on detecting broken payment flows, missing upstream links, weak beneficiary trails, duplicate identifiers, and abnormal timing. All features are deterministic, numeric, and grounded in visible columns. The plan prioritizes flow integrity, approval status, and final payment trail completeness.

## Runtime Sources And Feature Plans

### dak

- ID column: `id`
- Amount column: `amount`
- Context columns: `dakid_no, list_no, reference_no, bill_no, fk_section, fk_central_unit, fk_unit, fk_dak_type, month_year, record_status, subject, kpi_bill_type, mode_of_receipt, gem_bill, multiple_entry`
- Date column: `list_date`
- Why this source: dak is the root table for THIRD_PARTY_PAYMENTS (fk_section IN (142, 228, 265, 383)), contains primary payment identity (dakid_no), key financials (amount), and critical context (bill_no, reference_no, list_date, unit, section, status). It anchors the entire payment flow and is required for downstrea

Feature plan:
- `dak_missing_dakid_no` (`is_missing`): DAK identifier is required for traceability; missing dakid_no indicates invalid or unregistered payment entry.
- `dak_missing_list_no` (`is_missing`): DAK list number is required for receipt traceability; missing list_no suggests incomplete or invalid DAK entry.
- `dak_missing_bill_no` (`is_missing`): Bill number is required for third-party payments; missing bill_no indicates incomplete or fake invoice identity.
- `dak_missing_reference_no` (`is_missing`): Reference number from vendor/authority is used as alternate document identifier; missing reference_no weakens audit trail.
- `dak_fis_prefix_29_in_normal_tpp` (`starts_with`): FIS document numbers starting with 29 indicate GEM online processing; their presence in normal TPP context suggests misclassification or bypass of standard workflow.
- `dak_gem_bill_flagged` (`equals`): Gem_bill flag indicates GEM online processing; its presence in normal TPP context may indicate bypass of standard approval flow.
- `dak_multiple_entry_flagged` (`equals`): Multiple entry flag indicates duplicate or split payment behavior; its presence may signal bill splitting or fraud.
- `dak_duplicate_dakid_no` (`duplicate_key`): Duplicate DAK IDs suggest data duplication or identity reuse, which may indicate fake or repeated payment entries.
- `dak_duplicate_bill_no` (`duplicate_key`): Duplicate bill numbers across DAKs indicate potential invoice reuse or splitting, a red flag for fraud.
- `dak_duplicate_reference_no` (`duplicate_key`): Duplicate reference numbers suggest reused or forged vendor documents, weakening audit integrity.
- `dak_duplicate_normalized_bill_no` (`duplicate_normalized_key`): Normalized duplicate bill numbers (ignoring case/punctuation) indicate possible invoice splitting or identity manipulation.
- `dak_duplicate_normalized_reference_no` (`duplicate_normalized_key`): Normalized duplicate reference numbers suggest forged or reused vendor documents across entries.
- `dak_duplicate_distinct_bill_no_unit` (`duplicate_distinct`): Same bill number used across different units suggests invoice reuse or splitting, a sign of potential fraud.
- `dak_duplicate_distinct_bill_no_amount` (`duplicate_distinct`): Same bill number with different amounts across DAKs suggests manipulation or splitting of payment.
- `dak_duplicate_distinct_bill_no_section` (`duplicate_distinct`): Same bill number across different sections (142, 228, 265, 383) suggests misuse of a single invoice for multiple payment types.
- `dak_rolling_window_bill_count_7d` (`rolling_window_count` params={'window_days': 7}): High count of same bill number within 7 days suggests possible bill splitting or repeated submission.
- `dak_rolling_window_bill_amount_sum_7d` (`rolling_window_amount_sum` params={'window_days': 7}): High total amount for same bill number within 7 days may indicate split payment or overpayment.
- `dak_ratio_to_group_median_amount_by_unit` (`ratio_to_group_median` by `fk_central_unit`): Amounts significantly above or below median per unit may indicate overpayment or underpayment, or vendor manipulation.
- `dak_ratio_to_group_median_amount_by_section` (`ratio_to_group_median` by `fk_section`): Amounts deviating from median per section may indicate abnormal spending or fraud.
- `dak_ratio_to_group_median_amount_by_bill_type` (`ratio_to_group_median` by `kpi_bill_type`): Amounts inconsistent with median by bill type may indicate misclassification or manipulation.
- `dak_group_rank_pct_amount_by_unit` (`group_rank_pct` by `fk_central_unit`): High percentile rank within unit suggests outlier payment, possibly overpayment or fraud.
- `dak_group_rank_pct_amount_by_section` (`group_rank_pct` by `fk_section`): High percentile rank within section suggests abnormal payment size, possibly fraudulent.
- `dak_group_frequency_amount_by_unit` (`group_frequency` by `fk_central_unit`): High frequency of same amount within unit may indicate repeated or fake payments.
- `dak_group_frequency_amount_by_section` (`group_frequency` by `fk_section`): High frequency of same amount within section may indicate patterned fraud or manipulation.
- `dak_record_status_invalid` (`equals`): Invalid record status (I) indicates the DAK is not active; such records should not proceed to payment stages.
- `dak_record_status_cancelled` (`equals`): Cancelled DAKs should not be processed further; their presence in downstream flow is suspicious.
- `dak_record_status_rejected` (`equals`): Rejected DAKs should not move forward; any downstream flow from rejected DAKs is a red flag.
- `dak_record_status_valid` (`equals`): Valid DAKs are expected to proceed; lack of downstream flow may indicate blocked or hidden payment.

### bill

- ID column: `id`
- Amount column: `amount_claimed`
- Context columns: `fk_dak, fk_central_vendor, fk_central_unit, invoice_number, invoice_date, payment_mode, payment_record_type, gst_applicable, fk_bill_type, passed, approved, record_status, created_at, dv_no, cmp_date, dp_sheet_no, dp_sheet_date, cmp_batch`
- Date column: `created_at`
- Why this source: The bill table is the central transactional record for third-party payments, containing all key forensic signals: amount, identity, approval, payment flow, and date context. It links directly to DAK and downstream payment tables via FKs. It is the primary source for detecting anomalies in flow, appr

Feature plan:
- `is_invoice_future_date` (`date_in_future`): Invoice date must not be in the future. A future invoice date violates business rules and may indicate fake or backdated invoices.
- `is_invoice_after_bill_date` (`date_after`): Invoice date must not be after bill creation date. This ensures chronological integrity of the payment claim.
- `is_bill_date_future` (`date_in_future`): Bill creation date must not be in the future. A future bill date indicates invalid or manipulated data.
- `is_bill_approved_but_not_passed` (`numeric_gt`): An approved bill must be passed to proceed. A bill that is approved but not passed may be stuck or manipulated.
- `is_bill_passed_without_approved` (`numeric_gt`): A passed bill must be approved. This feature detects unapproved bills that are incorrectly marked as passed.
- `is_bill_invalid_status` (`equals`): Invalid record status indicates the bill is not valid for payment. Any downstream processing of invalid bills is suspicious.
- `is_bill_without_vendor` (`missing_when_equals`): Vendor payment type (V) requires a selected vendor. Missing vendor for V-type bills is a critical validation failure.
- `is_bill_without_bill_type` (`is_missing`): Bill type is required. Missing bill type makes the bill invalid and should not proceed.
- `is_duplicate_invoice_across_daks` (`duplicate_distinct`): Same invoice identity appearing across multiple DAKs suggests possible bill splitting or fraud.
- `is_invoice_splitting_nearby_dates` (`rolling_window_count` params={'window_days': 7}): Multiple bills with same vendor and invoice number within 7 days suggest possible invoice splitting to evade detection.
- `is_invoice_splitting_nearby_amounts` (`rolling_window_amount_sum` params={'window_days': 7}): Sum of claimed amounts for same invoice across nearby dates exceeds expected total, indicating possible splitting.
- `is_vendor_amount_above_median` (`ratio_to_group_median` by `fk_central_vendor`): Amount claimed by a vendor significantly above the median for that vendor may indicate overpayment or manipulation.
- `is_unit_amount_above_median` (`ratio_to_group_median` by `fk_central_unit`): Amount claimed by a unit significantly above the median for that unit may indicate overpayment or misuse.
- `is_bill_type_amount_above_median` (`ratio_to_group_median` by `fk_bill_type`): Amount claimed for a bill type significantly above the median may indicate anomaly in that category.
- `is_payment_mode_amount_above_median` (`ratio_to_group_median` by `payment_mode`): Amount claimed for a payment mode significantly above the median may indicate misuse or fraud.
- `is_vendor_amount_rank_high` (`group_rank_pct` by `fk_central_vendor`): High percentile rank of amount claimed by a vendor indicates outlier behavior.
- `is_unit_amount_rank_high` (`group_rank_pct` by `fk_central_unit`): High percentile rank of amount claimed by a unit indicates outlier behavior.
- `is_bill_type_amount_rank_high` (`group_rank_pct` by `fk_bill_type`): High percentile rank of amount claimed for a bill type indicates outlier behavior.
- `is_payment_mode_amount_rank_high` (`group_rank_pct` by `payment_mode`): High percentile rank of amount claimed for a payment mode indicates outlier behavior.

### cheque_slip

- ID column: `id`
- Amount column: `amount`
- Context columns: `fk_dak, fk_central_vendor, fk_central_beneficiary, fk_central_favour_bank, fk_ecs_payment_mode, payment_detail, approved, record_status, cheque_slip_date, dv_no, dp_sheet_no`
- Date column: `cheque_slip_date`
- Why this source: cheque_slip is the final payment stage in the third-party payment flow; it contains the actual payment amount, beneficiary, vendor, bank, payment mode, and approval status. It is the most reliable source for detecting anomalies in payment execution, final routing, and downstream flow completion.

Feature plan:
- `is_missing_dak_link` (`is_missing`): A cheque slip without a valid fk_dak indicates a payment record not linked to any DAK, breaking the upstream flow and suggesting a fake or orphaned payment.
- `is_missing_vendor` (`is_missing`): Missing vendor reference in a cheque slip implies a payment to an unverified or non-existent entity, violating vendor identity integrity in third-party payments.
- `is_missing_beneficiary` (`is_missing`): Missing beneficiary reference in a cheque slip indicates a payment with no defined recipient, raising red flags for unverified or diverted payments.
- `is_missing_favour_bank` (`is_missing`): Missing favour bank reference in a cheque slip suggests a payment without a defined bank account, which is a critical gap in payment routing and auditability.
- `is_missing_payment_mode` (`is_missing`): Missing payment mode in a cheque slip indicates a payment with undefined routing method, which can mask payment type manipulation or bypass of standard payment controls.
- `is_missing_payment_detail` (`is_missing`): Missing payment narration in a cheque slip reduces transparency and auditability, making it harder to verify the purpose and legitimacy of the payment.
- `is_not_approved` (`equals`): Cheque slips that are not approved should not be processed; this flag detects unapproved payments that may have bypassed the formal approval workflow.
- `is_not_valid_record_status` (`equals`): Only records with record_status = 'V' are valid; any other status indicates a rejected, invalid, or pending record that should not be part of the final payment flow.
- `cheque_slip_date_in_future` (`date_in_future`): A future cheque slip date violates payment chronology and may indicate date manipulation or fake payment timing.
- `duplicate_cheque_slip_dv_no` (`duplicate_key`): Duplicate DV numbers in cheque slips suggest possible duplicate payments or invoice splitting across multiple slips.
- `duplicate_cheque_slip_dp_sheet_no` (`duplicate_key`): Duplicate DP sheet numbers indicate possible duplicate payment processing or re-entry of the same payment.
- `duplicate_cheque_slip_vendor` (`duplicate_key`): Multiple cheque slips to the same vendor may indicate bulk or repeated payments, which could signal invoice splitting or abuse of vendor relationships.
- `duplicate_cheque_slip_beneficiary` (`duplicate_key`): Multiple payments to the same beneficiary may indicate repeated or duplicate payments, especially if amounts are similar.
- `duplicate_normalized_cheque_slip_vendor` (`duplicate_normalized_key`): Same vendor referenced across multiple cheque slips with different formatting or IDs may indicate identity obfuscation or split payments.
- `duplicate_normalized_cheque_slip_beneficiary` (`duplicate_normalized_key`): Same beneficiary referenced across multiple cheque slips with different formatting may indicate identity manipulation or payment diversion.
- `rolling_window_vendor_payment_count_7d` (`rolling_window_count` params={'window_days': 7}): High frequency of payments to the same vendor within 7 days may indicate bill splitting, repeated payments, or abuse of vendor relationships.
- `rolling_window_vendor_payment_amount_sum_7d` (`rolling_window_amount_sum` params={'window_days': 7}): Large cumulative payment amounts to the same vendor within 7 days may indicate invoice splitting or excessive payments to a single entity.
- `ratio_to_vendor_median_amount` (`ratio_to_group_median` by `fk_central_vendor`): A payment amount significantly higher or lower than the median vendor payment may indicate overpayment, underpayment, or manipulation.
- `vendor_payment_amount_rank_pct` (`group_rank_pct` by `fk_central_vendor`): A payment amount at extreme percentile rank within a vendor’s payment history may indicate outlier behavior or manipulation.
- `vendor_payment_frequency_log` (`group_frequency` by `fk_central_vendor`): High frequency of payments to a vendor (log-transformed) may indicate abuse or repeated use of the same vendor for suspicious payments.
- `payment_detail_contains_fis_29_prefix` (`starts_with`): FIS document number starting with '29' indicates GEM online processing; its presence in a normal TPP payment may suggest misuse of GEM system for non-GEM payments.
- `payment_detail_contains_fis_29_prefix_with_non_gem_payment_mode` (`missing_when_equals`): If payment detail contains FIS '29' prefix but payment mode is not GEM (e.g., ECS), it suggests misuse of GEM identifier in non-GEM context.

### punching_medium

- ID column: `id`
- Amount column: `amount`
- Context columns: `fk_dak, pm_date, record_status, approved, cancelled, sign_rc, detail_code_head, pm_section, category_head, code_head, detail_head, voucher_class, fk_central_unit, fk_office_id, fk_auditor, fk_aao, fk_ao, voucher_no, pm_month_year, cmp_batch, upload_batch, ncs_transaction_id`
- Date column: `pm_date`
- Why this source: punching_medium is the central payment flow table in the third-party payment workflow, with direct FK to dak, and contains critical payment attributes including amount, date, status, approval, and payment routing details. It is the primary source for detecting flow breaks, approval contradictions, a

Feature plan:
- `broken_payment_flow_downstream_without_upstream` (`is_missing`): A punching_medium row without a linked dak indicates a downstream payment record with no upstream bill or DAK, which violates the normal flow and suggests a fake or orphaned payment entry.
- `payment_with_invalid_status` (`equals`): Record status 'X' indicates invalid or deleted records; any payment with this status should not be processed, and its presence in the payment flow is a red flag.
- `payment_without_valid_sign_rc` (`equals`): Only '+C' sign_rc rows are valid for charged amount aggregation; any payment with a different sign_rc (e.g., '-C', 'X') is invalid and should not be included in payment totals.
- `duplicate_dak_payment` (`duplicate_key`): Multiple punching_medium entries for the same DAK indicate duplicate payment attempts or splitting of a single payment, which may signal bill splitting or fraud.
- `duplicate_normalized_dak_payment` (`duplicate_normalized_key`): Same DAK ID appearing with minor formatting differences (e.g., leading zeros, case) across multiple rows suggests possible duplicate or manipulated payment entries.
- `payment_date_in_future` (`date_in_future`): A payment date in the future violates basic chronology and suggests manipulation or data entry error.
- `high_amount_ratio_to_dak_amount` (`ratio_to_group_median` by `fk_dak`): A punching_medium amount significantly higher than the median amount for the same DAK may indicate overpayment or incorrect amount entry.
- `low_amount_ratio_to_dak_amount` (`ratio_to_group_median` by `fk_dak` params={'quantile': 0.05}): A punching_medium amount below the 5th percentile of the DAK's payment amounts may indicate underpayment or payment splitting.
- `abnormal_payment_frequency_per_dak` (`rolling_window_count` params={'window_days': 7}): Multiple payments for the same DAK within a 7-day window may indicate bill splitting or repeated payment attempts.
- `abnormal_payment_amount_sum_per_dak` (`rolling_window_amount_sum` params={'window_days': 7}): A high sum of payments for the same DAK within 7 days may indicate an attempt to split a large payment to bypass approval limits.
- `payment_with_no_approval_trail` (`missing_when_equals`): If a DAK is approved, but its associated punching_medium payment is not marked as approved, it indicates a broken approval trail and potential bypass of controls.
- `payment_with_invalid_detail_code_head` (`starts_with`): Detail code head starting with '000' is invalid and may indicate a placeholder or dummy entry, suggesting a fake payment.

### schedule3

- ID column: `id`
- Amount column: `schedule3_amount`
- Context columns: `fk_dak, fk_cheque_slip, fk_section, section_code, dp_sheet_date, month, transcription_type, cmp_batch`
- Date column: `dp_sheet_date`
- Why this source: schedule3 is the final payment stage in the third-party payment flow; it contains the final amount, date, and linkage to the cheque slip and DAK. It is critical for detecting broken payment trails, final payment anomalies, and downstream flow violations.

Feature plan:
- `broken_payment_flow_downstream_without_upstream` (`is_missing`): A schedule3 row without a linked cheque_slip indicates a payment record that has no valid payment slip, suggesting a broken or fake payment flow.
- `schedule3_payment_without_dak_link` (`is_missing`): schedule3 rows without a linked DAK are disconnected from the original payment request, indicating a potential fake or unauthorized payment.
- `schedule3_payment_with_future_dp_sheet_date` (`date_in_future`): A payment date in the future violates normal payment chronology and suggests manipulation or incorrect data entry.
- `schedule3_payment_with_duplicate_dak` (`duplicate_key`): Multiple schedule3 entries for the same DAK indicate duplicate payments or payment splitting, a red flag for fraud or manipulation.
- `schedule3_payment_with_duplicate_cheque_slip` (`duplicate_key`): Multiple schedule3 entries for the same cheque slip indicate duplicate payment records, a strong signal of overpayment or fraud.
- `schedule3_payment_with_duplicate_dak_cheque_slip_pair` (`duplicate_key`): A duplicate DAK-cheque_slip pair in schedule3 indicates a potential duplicate payment or data corruption.
- `schedule3_payment_with_abnormal_transcription_type` (`starts_with`): Transcription type 'R' may indicate a reprocessed or corrected entry, which is suspicious if not expected in the normal flow.
- `schedule3_payment_with_abnormal_cmp_batch` (`group_frequency` by `cmp_batch`): A CMP batch with an unusually high number of schedule3 entries may indicate batch manipulation or overpayment.
- `schedule3_payment_with_abnormal_month` (`group_frequency` by `month`): A month with an unusually high number of schedule3 entries may indicate batch processing or manipulation.
- `schedule3_payment_with_abnormal_section_code` (`group_frequency` by `section_code`): A section code with an unusually high number of schedule3 entries may indicate a concentration of suspicious payments.

### ecs

- ID column: `id`
- Amount column: `amount`
- Context columns: `fk_dak, fk_schedule3, fk_cheque_slip, fk_central_beneficiary, fk_bank_pan_detail, fk_ecs_payment_mode, payment_reference_no, cmp_reference_no, utr_no, month, record_status, approved, to_be_paid, released, cmp_payment_status`
- Date column: `cmp_file_gen_date`
- Why this source: ECS is the final payment stage in the third-party payment flow; it contains the last executable payment record with full traceability to DAK, schedule3, and cheque_slip. It is the most reliable source for detecting broken payment trails, weak final payment evidence, and downstream anomalies.

Feature plan:
- `ecs_broken_payment_flow` (`missing_when_present`): A valid ECS payment without a linked schedule3 row indicates a broken payment flow; schedule3 is a required upstream stage in the normal flow. This signals potential bypass of financial verification.
- `ecs_missing_cheque_slip_link` (`missing_when_present`): ECS payment without a linked cheque_slip suggests a bypass of the cheque issuance stage, which is a critical control point in the payment chain. This may indicate unauthorized or synthetic payment routing.
- `ecs_final_payment_without_approval` (`missing_when_equals`): ECS records with valid status (V) but not approved indicate a final payment without proper authorization, violating the approval workflow and increasing fraud risk.
- `ecs_duplicate_payment_reference` (`duplicate_key`): Duplicate payment reference numbers across ECS records suggest possible duplicate payments or manipulation of payment identifiers, a red flag for payment fraud.
- `ecs_duplicate_utr_no` (`duplicate_key`): Duplicate UTR numbers in ECS records indicate potential duplicate fund transfers or fake payment confirmations, a strong signal of payment duplication or spoofing.
- `ecs_duplicate_cmp_reference` (`duplicate_key`): Duplicate CMP reference numbers suggest possible reprocessing of the same payment, which may indicate attempts to bypass payment limits or audit controls.
- `ecs_unusual_payment_status` (`starts_with`): ECS records with CMP payment status starting with 'R' (e.g., rejected, returned) but still marked as released or to_be_paid indicate a contradiction between status and execution, suggesting manipulation.
- `ecs_payment_without_beneficiary` (`missing_when_present`): ECS payment without a beneficiary linked to the DAK indicates a potential diversion of funds to an unauthorized party, violating the vendor-to-beneficiary alignment rule.
- `ecs_payment_with_mismatched_bank_pan` (`missing_when_present`): ECS payment without a linked bank PAN detail for the beneficiary suggests a weak payment trail, increasing risk of fund diversion or fake beneficiary creation.
- `ecs_payment_mode_mismatch` (`equals`): ECS payments using payment mode 1 (e.g., ECS) without a corresponding cheque slip or schedule3 approval may indicate bypass of physical payment controls, especially if the mode is inconsistent with the bill's original mode.
- `ecs_payment_with_weak_trail` (`all_missing`): ECS records missing all key upstream and beneficiary links indicate a completely broken payment trail, making the payment unverifiable and highly suspicious.
- `ecs_payment_with_high_frequency_nearby` (`rolling_window_count` params={'window_days': 7}): Multiple ECS payments for the same DAK within a 7-day window may indicate bill splitting or repeated payment attempts, a proxy for evasion of approval or payment limits.
- `ecs_payment_with_high_amount_sum_nearby` (`rolling_window_amount_sum` params={'window_days': 7}): High cumulative ECS payment amount for the same DAK within a short window may indicate attempted overpayment or splitting of a large payment to avoid scrutiny.
